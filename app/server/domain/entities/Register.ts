
// Crie a classe Register representando a Camada de domínio

export class Register {
    // Crie as propriedades do registro
    id: number;
    firstName: string;
    lastName: string;
    email: string;
    password: string;
    phone?: string;
    userCode?: number
    createdAt?: Date;
    updatedAt?: Date;
}

